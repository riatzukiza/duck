There's no way I'm getting the NPU working through docker.
Docker is just not designed for  this kinda thing.
If they are gonna do tesnor compute in the cloud, their going to use a larger gpu.
Not these NPUs

The NPU's strength is not it's speed, it's latency.
It's directly on the CPU die.
It shares the same  memory as the CPU.

This  goes in the icebox with the other NPU tasks
